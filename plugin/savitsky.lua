-- savitsky.nvim plugin initialization
-- This file is automatically loaded by Neovim

if vim.g.loaded_savitsky then
  return
end
vim.g.loaded_savitsky = 1

-- The plugin is now loaded and can be used via require("savitsky")
